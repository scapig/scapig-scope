
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

POST    /scope                      @controllers.ScopeController.createOrUpdate()

GET     /scope/:key                 @controllers.ScopeController.fetch(key)

GET     /scope                      @controllers.ScopeController.fetchMultiple(keys)
